name: Audio Bookshelf UI - Test Suite

on:
  push:
    branches: [ main, develop, feature/* ]
  pull_request:
    branches: [ main, develop ]

jobs:
      
  test-android:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.16.0'
        channel: 'stable'
        
    - name: Setup Java
      uses: actions/setup-java@v3
      with:
        distribution: 'zulu'
        java-version: '17'
        
    - name: Install dependencies
      run: flutter pub get
      
    - name: Verify dependencies
      run: flutter pub deps
      
    - name: Run unit tests
      run: flutter test test/core/ test/domain/ test/application/ --coverage
      
    - name: Run widget tests
      run: flutter test test/presentation/ --coverage
      
    - name: Run integration tests
      run: flutter test test/integration/ --coverage
      
    - name: Generate coverage report
      run: |
        flutter test --coverage
        genhtml coverage/lcov.info -o coverage/html
        
    - name: Upload coverage reports
      uses: actions/upload-artifact@v4
      with:
        name: android-coverage-report
        path: coverage/html/
        
    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v3
      with:
        file: coverage/lcov.info
        flags: android-tests
        name: android-coverage
        
  accessibility-test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.16.0'
        channel: 'stable'
        
    - name: Install dependencies
      run: flutter pub get
      
    - name: Run accessibility tests
      run: flutter test test/accessibility/ --coverage
      
    - name: Generate accessibility report
      run: |
        flutter test --coverage
        genhtml coverage/lcov.info -o coverage/html
        
    - name: Upload accessibility report
      uses: actions/upload-artifact@v4
      with:
        name: accessibility-report
        path: coverage/html/
        
  test-summary:
    runs-on: ubuntu-latest
    needs: [test-android, accessibility-test]
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.16.0'
        channel: 'stable'
        
    - name: Install dependencies
      run: flutter pub get
      
    - name: Run all tests
      run: flutter test --coverage
      
    - name: Generate comprehensive coverage report
      run: |
        genhtml coverage/lcov.info -o coverage/html
        echo "Coverage report generated successfully"
        
    - name: Upload comprehensive coverage report
      uses: actions/upload-artifact@v4
      with:
        name: android-accessibility-coverage-report
        path: coverage/html/
        
    - name: Comment coverage on PR
      uses: actions/github-script@v6
      if: github.event_name == 'pull_request'
      with:
        script: |
          const fs = require('fs');
          const path = require('path');
          
          // Read coverage summary
          const coverageFile = path.join('coverage', 'lcov.info');
          if (fs.existsSync(coverageFile)) {
            const coverage = fs.readFileSync(coverageFile, 'utf8');
            const lines = coverage.split('\n');
            let totalLines = 0;
            let coveredLines = 0;
            
            for (const line of lines) {
              if (line.startsWith('LF:')) {
                totalLines += parseInt(line.split(':')[1]);
              } else if (line.startsWith('LH:')) {
                coveredLines += parseInt(line.split(':')[1]);
              }
            }
            
            const percentage = totalLines > 0 ? (coveredLines / totalLines * 100).toFixed(2) : 0;
            
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `## Android & Accessibility Test Coverage Report
              
              **Total Coverage**: ${percentage}%
              **Covered Lines**: ${coveredLines}
              **Total Lines**: ${totalLines}
              
              Coverage report includes Android tests and accessibility tests.
              Report is available in the artifacts.`
            });
          }
